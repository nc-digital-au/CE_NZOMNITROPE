import { KeyValueIntPipe } from './key-value-int.pipe';

describe('KeyValueIntPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyValueIntPipe();
    expect(pipe).toBeTruthy();
  });
});
